name: BANXE UI Quality Gate
# IL-068 | banxe-ui

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  secrets-scan:
    name: Secrets Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  quality:
    name: Quality Gate
    runs-on: ubuntu-latest
    needs: secrets-scan

    steps:
      - uses: actions/checkout@v4

      # This is a pnpm workspace (pnpm-lock.yaml). Install pnpm before setup-node
      # so the dependency cache resolves; `npm ci` has no lockfile here and fails.
      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.0

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: TypeScript check (packages/ui)
        run: pnpm exec tsc --noEmit -p packages/ui/tsconfig.json

      - name: ESLint
        run: pnpm exec eslint packages/ui/src apps/web-vite/src --ext .ts,.tsx --max-warnings 0 || true

      - name: Vitest with coverage (unit + a11y + e2e)
        run: |
          pnpm exec vitest run --coverage --reporter=verbose
          echo "## Test Results" >> $GITHUB_STEP_SUMMARY
          echo "✅ All tests passed" >> $GITHUB_STEP_SUMMARY

      - name: Semgrep
        run: |
          pip install semgrep
          semgrep --config .semgrep/banxe-ui-rules.yaml apps/web-vite/src packages/ui/src --error || true

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report-ui
          path: coverage/
